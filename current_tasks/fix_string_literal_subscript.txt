Task: Fix string literal direct subscript element size and type

Problem: "string"[index] produces wrong results because:
1. get_array_elem_size returns 8 (default) for StringLiteral base, should return 1
2. get_expr_type for ArraySubscript with StringLiteral base returns I64, should return I8
3. get_expr_ctype has no arm for StringLiteral

Fix plan:
- Add StringLiteral handling to get_array_elem_size / get_array_elem_size_for_subscript
- Add StringLiteral to get_expr_ctype returning pointer-to-char or array-of-char
- Ensure get_expr_type for subscript resolves to I8 for string literal bases

Status: IN PROGRESS
