Task: Clean up ARM linker to use shared linker_common infrastructure

The ARM linker (arm/linker/mod.rs, 3512 lines) re-implements many functions
that already exist in linker_common.rs and are used by the x86 linker.

Plan:
1. Remove duplicate OutputSection/InputSection structs from ARM linker
   (use linker_common versions)
2. Replace ARM's merge_sections() with linker_common::merge_sections_elf64_gc()
3. Replace ARM's register_symbols() with linker_common::register_symbols_elf64()
4. Replace ARM's allocate_common_symbols() with linker_common::allocate_common_symbols_elf64()
5. Replace ARM's load_archive()/load_thin_archive() with linker_common versions
6. Extract shared create_plt_got logic (copy reloc alias detection, GOT building)
7. Extract shared .gnu.hash building, dyn_sym_names building, resolve_sym
8. Clean up and verify no regressions

Started: 2026-02-05
